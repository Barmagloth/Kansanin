# Architecture

## Components

This service handles all incoming HTTP requests.

These components are deployed as separate containers.

That configuration file contains the database credentials.

Those endpoints are restricted to authenticated users only.

The system shall validate this request before processing.
