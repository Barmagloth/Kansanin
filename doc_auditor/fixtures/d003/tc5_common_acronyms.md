# Requirements

## Functional Requirements

The API shall return JSON responses for all REST endpoints.

The system must support HTTP and HTTPS protocols.

All data shall be stored in SQL-compatible databases.
