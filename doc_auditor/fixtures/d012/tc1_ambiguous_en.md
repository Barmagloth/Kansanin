# System Requirements

## Data Processing

The service sends the token to the gateway and it validates it.

The system shall forward the request to the handler and the controller must process it before returning.

## Authentication

The authentication module shall verify the credential against the database, and it shall reject it if invalid.
